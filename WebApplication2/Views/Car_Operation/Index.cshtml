@model IEnumerable<WebApplication2.Models.Car_Operation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TimeDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CarType)
        </th>
       @* <th>
        @Html.DisplayNameFor(model => model.Sign.Name)
    </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TimeDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarType)
        </td>
      @*  <td>
            @Html.DisplayFor(modelItem => item.Sign.Name)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoID })
        </td>
    </tr>
}

</table>
