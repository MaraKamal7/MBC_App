@model IEnumerable<WebApplication2.Models.Bike>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BikeType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BikeModel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BikeRecipet)
        </th>
       @* <th>
        @Html.DisplayNameFor(model => model.Sign.Name)
    </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BikeType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BikeModel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BikeRecipet)
        </td>
      @* <td>
            @Html.DisplayFor(modelItem => item.Sign.Name)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BikeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BikeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BikeID })
        </td>
    </tr>
}

</table>
